'use strict';

/**
 * vehicle-image-list service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::vehicle-image-list.vehicle-image-list');
